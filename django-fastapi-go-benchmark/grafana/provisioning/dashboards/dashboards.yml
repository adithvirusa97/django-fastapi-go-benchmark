apiVersion: 1
providers:
  - name: benchmark
    orgId: 1
    folder: Benchmark
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options: {path: /var/lib/grafana/dashboards}
